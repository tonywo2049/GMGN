interface:
  display_name: "Release / 版本发布"
  short_description: "Publish an accepted commit without repeating closure review / 复用已接受 commit 的证据完成发布，不重复关账审查"
  default_prompt: "Use $release to package and publish the accepted commit with reusable commit-bound evidence. / 请用 $release 复用绑定 commit 的证据打包并发布已接受候选。"
