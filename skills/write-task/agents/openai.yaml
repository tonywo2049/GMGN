interface:
  display_name: "Write Tasks / 拆分任务"
  short_description: "Split minimal executable and acceptable Tasks / 拆分最小可执行、可验收 Task"
  default_prompt: "Use $write-task to create or revise Task.md from the reviewed Design. / 请用 $write-task 根据已评审的 Design 创建或修改 Task.md。"
