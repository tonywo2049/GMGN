interface:
  display_name: "Run Ready Tasks / 执行 Ready 任务卡"
  short_description: "Continuously run ready cards in isolated lanes, then integrate and close / 滚动并行执行 ready 卡并串行集成关账"
  default_prompt: "Use $run-task to execute the confirmed Task set under its Card, readiness, review, integration, and closure rules. / 请用 $run-task 按 Card、就绪、审查、集成与关闭规则执行已确认的 Task 集。"
